<?xml version="1.0" encoding="UTF-8"?>
<LightningComponentBundle xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>66.0</apiVersion>
    <isExposed>true</isExposed>
    <masterLabel>Agentforce Record Insights</masterLabel>
    <description>AI-powered conversational insights grounded in live Salesforce record data. Drop on any record page or app page.</description>
    <targets>
        <target>lightning__RecordPage</target>
        <target>lightning__AppPage</target>
        <target>lightning__HomePage</target>
    </targets>
    <targetConfigs>
        <targetConfig targets="lightning__RecordPage">
            <property name="cardTitle" type="String" default="Agentforce Record Insights" label="Card Title" description="Controls the title shown at the top of the component. Example: Opportunity Insights Assistant." />
            <property name="defaultDepth" type="Integer" default="1" label="Default Relationship Depth" description="Sets the starting relationship depth for insights and compare mode. Use 1 for direct child records only, 2 for children plus grandchildren, or 3 for the deepest context." />
            <property name="defaultMode" type="String" default="insights" label="Default Mode" datasource="insights,compare" description="Chooses which experience opens first when both modes are available. Select Insights for chat on the current record or Compare for side-by-side record analysis." />
            <property name="availableModes" type="String" default="both" label="Available Modes" datasource="both,insightsOnly,compareOnly" description="Controls which modes users can access. Both shows Insights and Compare, insightsOnly shows only the record chat experience, and compareOnly shows only the compare experience." />
            <property name="preloadCompareMode" type="Boolean" default="true" label="Preload Compare Mode" description="If enabled, compare mode initializes in the background when the record page loads so object support checks, search, and compare settings are ready sooner when users switch to Compare. Turn this off to defer compare setup until Compare is opened, which reduces upfront page load work but makes compare mode start more slowly." />
            <property name="startWithContextPanelOpen" type="Boolean" default="true" label="Start With Context Panel Open" description="If enabled, the context settings panel is expanded when the component first loads in insights mode." />
            <property name="maxDepthAllowed" type="Integer" default="3" min="1" max="3" label="Maximum Relationship Depth" description="Limits the highest depth users can select in insights and compare mode. Set this to 1, 2, or 3 to control how much related-record context can be included." />
            <property name="defaultFieldCategoriesCsv" type="String" label="Default Field Categories (CSV)" description="Optional comma-separated field category API names to preselect in insights and compare mode. Valid categories are core, financial, status, dates, relationships, custom, and other. Example: core,status,financial,custom." />
            <property name="defaultRelationshipsCsv" type="String" label="Default Relationships (CSV)" description="Optional comma-separated child relationship API names to preselect in insights and compare mode. Values depend on the object. Example for Account: Contacts,Opportunities,Cases." />
            <property name="defaultParentReferencesCsv" type="String" label="Default Parent Records (CSV)" description="Optional comma-separated lookup field API names on the current object to preselect as parent records. One-hop only. Example for a custom Order: AccountId,ContactId,TreatmentSite__c." />
            <property name="defaultSameObjectSiblingsEnabled" type="Boolean" default="false" label="Preload Same-Object Siblings Through Parents" description="If enabled, when a parent record is included, the component also loads other records of the same object type under that parent (excluding the active record). Increases prompt size; keep off unless sibling comparisons are core to the use case." />
            <property name="maxParentReferencesSelected" type="Integer" default="5" min="1" max="10" label="Maximum Parent Records Selected" description="Caps how many parent lookup records a user can include at once. Lower this to keep prompts small and focused." />
            <property name="showModelPicker" type="Boolean" default="true" label="Show Model Picker" description="If enabled, users can choose the AI model from the header of the chat panel." />
            <property name="defaultModelApiName" type="String" label="Default Model API Name" description="Optional model API name to preselect when it is available in the org. Example: sfdc_ai__DefaultVertexAIGeminiPro31 or sfdc_ai__DefaultGPT4Omni." />
            <property name="showSuggestedPrompts" type="Boolean" default="true" label="Show Suggested Prompts" description="If enabled, the welcome state shows built-in starter prompts that users can click to begin the conversation." />
            <property name="showUsageMetrics" type="Boolean" default="true" label="Show Usage Metrics" description="If enabled, the component shows token and flex-credit usage estimates in insights and compare mode." />
            <property name="showInlineUsageStatus" type="Boolean" default="false" label="Show Chat Usage Status" description="If enabled, the chat panel shows a compact usage footer near the message input with cumulative session tokens and flex credits. Leave this off to keep usage visible only in the context and settings areas." />
            <property name="promptWarningThresholdTokens" type="Integer" default="20000" min="0" label="Large Prompt Warning Threshold (Tokens)" description="Shows a warning before sending an estimated prompt larger than this many tokens in insights or compare mode. Set to 0 to disable the warning. Large prompts can be slower, use more flex credits, and may force some context to be truncated." />
            <property name="maxCompareRecords" type="Integer" default="5" min="2" max="5" label="Maximum Compare Records" description="Limits how many records users can select at once in compare mode. Lower this to reduce prompt size and keep comparisons focused." />
            <property name="relatedRecordsPerRelationship" type="Integer" default="10" min="1" max="20" label="Related Records Per Relationship" description="Controls how many child records are loaded for each selected relationship in insights and compare mode. Higher values provide richer context but increase tokens and response cost." />
            <property name="hideContextWarnings" type="Boolean" default="false" label="Hide Context Warnings" description="If enabled, the component suppresses the inline context warning banners in the context panel and chat panel." />
            <property name="persistConversation" type="Boolean" default="false" label="Persist Conversation" description="If enabled, conversation history and usage metrics are stored in browser localStorage only so they can be restored when the user returns to the same record on the same browser." />
            <property name="showSuggestedComparisonRecords" type="Boolean" default="true" label="Show Suggested Comparison Records" description="If enabled, compare mode shows suggested related records, such as sibling records or recently modified records of the same type." />
            <property name="enableSuggestedFollowUps" type="Boolean" default="true" label="Enable Suggested Follow-Ups" description="If enabled, the component generates suggested follow-up questions after each AI response. This makes an additional AI call and typically consumes about 2 extra flex credits per message turn." />
        </targetConfig>
        <targetConfig targets="lightning__AppPage,lightning__HomePage">
            <property name="cardTitle" type="String" default="Agentforce Record Insights" label="Card Title" description="Controls the title shown at the top of the component. Example: Pipeline Comparison Assistant." />
            <property name="defaultDepth" type="Integer" default="1" label="Default Relationship Depth" description="Sets the starting relationship depth for insights and compare mode after records are selected. Use 1 for direct child records only, 2 for children plus grandchildren, or 3 for the deepest context." />
            <property name="defaultMode" type="String" default="compare" label="Default Mode" datasource="insights,compare" description="Chooses which experience opens first when the component loads. Select Insights for record chat or Compare for side-by-side record analysis." />
            <property name="availableModes" type="String" default="both" label="Available Modes" datasource="both,insightsOnly,compareOnly" description="Controls which modes users can access. Both shows Insights and Compare, insightsOnly shows only the record chat experience, and compareOnly shows only the compare experience." />
            <property name="startWithContextPanelOpen" type="Boolean" default="true" label="Start With Context Panel Open" description="If enabled, the context settings panel is expanded when insights mode is opened." />
            <property name="maxDepthAllowed" type="Integer" default="3" min="1" max="3" label="Maximum Relationship Depth" description="Limits the highest depth users can select in insights and compare mode. Set this to 1, 2, or 3 to control how much related-record context can be included." />
            <property name="defaultFieldCategoriesCsv" type="String" label="Default Field Categories (CSV)" description="Optional comma-separated field category API names to preselect in insights and compare mode. Valid categories are core, financial, status, dates, relationships, custom, and other. Example: core,status,financial,custom." />
            <property name="defaultRelationshipsCsv" type="String" label="Default Relationships (CSV)" description="Optional comma-separated child relationship API names to preselect in insights and compare mode after records are selected. Values depend on the object. Example for Account: Contacts,Opportunities,Cases." />
            <property name="defaultParentReferencesCsv" type="String" label="Default Parent Records (CSV)" description="Optional comma-separated lookup field API names to preselect as parent records in insights and compare mode. One-hop only. Example for a custom Order: AccountId,ContactId,TreatmentSite__c." />
            <property name="defaultSameObjectSiblingsEnabled" type="Boolean" default="false" label="Preload Same-Object Siblings Through Parents" description="If enabled, when a parent record is included, the component also loads other records of the same object type under that parent (excluding the active record). Increases prompt size; keep off unless sibling comparisons are core to the use case." />
            <property name="maxParentReferencesSelected" type="Integer" default="5" min="1" max="10" label="Maximum Parent Records Selected" description="Caps how many parent lookup records a user can include at once. Lower this to keep prompts small and focused." />
            <property name="showModelPicker" type="Boolean" default="true" label="Show Model Picker" description="If enabled, users can choose the AI model from the header of the chat panel." />
            <property name="defaultModelApiName" type="String" label="Default Model API Name" description="Optional model API name to preselect when it is available in the org. Example: sfdc_ai__DefaultVertexAIGeminiPro31 or sfdc_ai__DefaultGPT4Omni." />
            <property name="showSuggestedPrompts" type="Boolean" default="true" label="Show Suggested Prompts" description="If enabled, the welcome state shows built-in starter prompts that users can click to begin the conversation." />
            <property name="showUsageMetrics" type="Boolean" default="true" label="Show Usage Metrics" description="If enabled, the component shows token and flex-credit usage estimates in insights and compare mode." />
            <property name="showInlineUsageStatus" type="Boolean" default="false" label="Show Chat Usage Status" description="If enabled, the chat panel shows a compact usage footer near the message input with cumulative session tokens and flex credits. Leave this off to keep usage visible only in the context and settings areas." />
            <property name="promptWarningThresholdTokens" type="Integer" default="20000" min="0" label="Large Prompt Warning Threshold (Tokens)" description="Shows a warning before sending an estimated prompt larger than this many tokens in insights or compare mode. Set to 0 to disable the warning. Large prompts can be slower, use more flex credits, and may force some context to be truncated." />
            <property name="maxCompareRecords" type="Integer" default="5" min="2" max="5" label="Maximum Compare Records" description="Limits how many records users can select at once in compare mode. Lower this to reduce prompt size and keep comparisons focused." />
            <property name="relatedRecordsPerRelationship" type="Integer" default="10" min="1" max="20" label="Related Records Per Relationship" description="Controls how many child records are loaded for each selected relationship in insights and compare mode. Higher values provide richer context but increase tokens and response cost." />
            <property name="hideContextWarnings" type="Boolean" default="false" label="Hide Context Warnings" description="If enabled, the component suppresses the inline context warning banners in the context panel and chat panel." />
            <property name="persistConversation" type="Boolean" default="false" label="Persist Conversation" description="If enabled, conversation history and usage metrics are stored in browser localStorage only so they can be restored when the user returns to the same record or app context on the same browser." />
            <property name="showSuggestedComparisonRecords" type="Boolean" default="true" label="Show Suggested Comparison Records" description="If enabled, compare mode shows suggested related records, such as sibling records or recently modified records of the same type." />
            <property name="enableSuggestedFollowUps" type="Boolean" default="true" label="Enable Suggested Follow-Ups" description="If enabled, the component generates suggested follow-up questions after each AI response. This makes an additional AI call and typically consumes about 2 extra flex credits per message turn." />
        </targetConfig>
    </targetConfigs>
</LightningComponentBundle>
